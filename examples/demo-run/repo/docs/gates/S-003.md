# Gates S-003 - Document CLI Wrapper

Frozen before implementation at `2026-07-01 16:05 Africa/Tunis`.

## Acceptance Gates

| Gate ID | Requirement | Verification command / method | Pass condition |
| --- | --- | --- | --- |
| `G-001` | README mentions `bin/judgeloop` commands. | Inspect `README.md`. | `init`, `doctor`, and `validate` are named. |
| `G-002` | CLI wrapper validates demo repo memory. | `python3 bin/judgeloop doctor examples/demo-run/repo` | Exits 0. |
| `G-003` | Package validation remains green. | `make validate` | Exits 0. |

## Out Of Scope

- Package publishing.
- Shell completion.
- `freeze` and `verify` commands.

## Freeze Rule

Builder edits to this file after freeze fail the slice unless the human
explicitly approves the change.
